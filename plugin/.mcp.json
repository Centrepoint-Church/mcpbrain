{
  "_comment": "The mcpbrain MCP server is registered by `mcpbrain setup` with the absolute install path — into BOTH claude_desktop_config.json (the chat app) and ~/.claude.json (Claude Code's own config, user scope), since staff use both surfaces. Neither reads its MCP servers from this plugin manifest. See mcpbrain/connector.py:register_connector.",
  "mcpServers": {}
}
