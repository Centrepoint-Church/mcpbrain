{
  "_comment": "The mcpbrain MCP server is registered by `mcpbrain setup` into the Claude Desktop config (claude_desktop_config.json) with the absolute install path — Claude Desktop, where the plugin runs, reads its MCP servers from there, not from this plugin manifest. See mcpbrain/setup.py:_register_desktop_mcp.",
  "mcpServers": {}
}
